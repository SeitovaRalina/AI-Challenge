LITELLM_API_KEY=
LITELLM_MODEL=
# Optional, defaults to https://llm.effective.land
LITELLM_BASE_URL=
# Optional, defaults to 8080
PORT=
# Optional, defaults to http://localhost:5173 (the Vite dev server)
CORS_ALLOWED_ORIGIN=
